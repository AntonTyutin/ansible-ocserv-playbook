---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install common packages
  apt:
    name:
      - htop
      - curl
      - wget
      - git
      - iproute2
    state: present

- name: Set timezone
  timezone:
    name: UTC
